{$DOMAIN:localhost} {
    # Proxy API calls to FastAPI backend (running inside bot process)
    handle /api/* {
        reverse_proxy bot:8080
    }

    # Serve React SPA — all other paths return index.html
    handle {
        root * /srv/webapp
        try_files {path} /index.html
        file_server
    }
}
